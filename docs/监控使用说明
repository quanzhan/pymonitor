#yum -y install python-setuptools
监控需要用到redis数据库。
但由于监控的数据库比较多，对应节点的数据采用了存入节点ip末尾加10的存储,所以需要配置redis数据库，并修改配置为300个数据库。
服务端启动需要配置redis的server及port。及服务启动的port。
客户端需要配置服务器的port。并配置需要监测的网卡名。(后期会优化，采用网络总数量为准)
